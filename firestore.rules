
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 協作模式：只要是通過身份驗證的使用者（包含匿名登入）皆可存取所有資料
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
